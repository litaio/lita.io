= wrap_layout :outer do
  .container
    .page-header
      %h1= current_page.data.title

    .row
      .col-md-3= partial "#{current_page.data.category}/menu"
      .col-md-9
        - if current_page.data.subtitle
          .page-header.first-heading
            %h2= current_page.data.subtitle

        = find_and_preserve do
          = yield
